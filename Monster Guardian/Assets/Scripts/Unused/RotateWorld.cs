using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RotateWorld : MonoBehaviour
{
    void Start()
    {
        // based on the player keys rotate the world
        // forward will rotate the world backwards to simulate movement
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}